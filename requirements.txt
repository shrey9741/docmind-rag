# ── Core UI ─────────────────────────────────────────────────────────────────
streamlit==1.36.0

# ── LangChain ecosystem ──────────────────────────────────────────────────────
langchain==0.2.5
langchain-community==0.2.5
langchain-huggingface==0.0.3
langchain-text-splitters==0.2.4

# ── HuggingFace / Transformers ───────────────────────────────────────────────
transformers==4.41.0
sentence-transformers==3.0.1
huggingface-hub==0.23.0
tokenizers==0.19.1

# ── Vector store ─────────────────────────────────────────────────────────────
faiss-cpu==1.8.0           # use faiss-gpu if you have a CUDA GPU

# ── Document parsing ─────────────────────────────────────────────────────────
PyPDF2==3.0.1              # PDF extraction
python-docx==1.1.0         # DOCX extraction

# ── ML / numerics ────────────────────────────────────────────────────────────
torch==2.3.0               # CPU build is fine; pip installs CPU by default
numpy==1.26.4

# ── Misc ─────────────────────────────────────────────────────────────────────
accelerate==0.30.0         # speeds up model loading
python-dotenv==1.0.1